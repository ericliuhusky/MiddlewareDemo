// swift-tools-version: 5.6

import PackageDescription

let package = Package(
    name: "Middleware",
    products: [
        .library(
            name: "Middleware",
            targets: ["Middleware"]),
    ],
    dependencies: [],
    targets: [
        .target(
            name: "Middleware",
            dependencies: [])
    ]
)
